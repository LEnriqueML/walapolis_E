<h2>Log in</h2>

<%= render 'layouts/shared/greetings' %>
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="field">
    <%= f.label :password %><br />
    <%= f.password_field :password, autocomplete: "off" %>
  </div>

  <% if devise_mapping.rememberable? -%>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <% end -%>

  <div class="actions">
    <%= f.submit "Log in" %>
  </div>

  <div class="form-group text-center">
    <%= link_to "He olvidado mi contrsaeña", new_user_password_path%>
  </div>

  <hr>
<div class="row">
  <div class="col-md-6">
    <%= link_to "<i class='fab fa-facebook'></i> Entrar con Facebook".html_safe, user_facebook_omniauth_authorize_path, class: 'btn btn-success btn-block' %>
  </div>
  <div class="col-md-6">
    <%= link_to "<i class='fab fa-google'></i> Entrar con Google".html_safe, user_google_oauth2_omniauth_authorize_path, class: 'btn btn-success btn-block' %>
  </div>
</div>
<% end %>

<%= render "users/shared/links" %>
